
## Active Technologies
- Bash 4.0+ (POSIX-compatible where possible) with `set -e` error handling + `git` (2.25+), `gh` (GitHub CLI 2.0+), `bd` (beads CLI), `jq` (JSON parsing) (001-bkff-plugin)
- File-based (markdown files, JSON for structured output, `.beads/` directory for issue tracking) (001-bkff-plugin)

## Recent Changes
- 001-bkff-plugin: Added Bash 4.0+ (POSIX-compatible where possible) with `set -e` error handling + `git` (2.25+), `gh` (GitHub CLI 2.0+), `bd` (beads CLI), `jq` (JSON parsing)
- 001-bkff-plugin: Added Bash 4.0+ (POSIX-compatible where possible) with `set -e` error handling + `git` (2.25+), `gh` (GitHub CLI 2.0+), `bd` (beads CLI), `jq` (JSON parsing)
- 001-bkff-plugin: Added Bash 4.0+ (POSIX-compatible where possible) with `set -e` error handling + `git` (2.25+), `gh` (GitHub CLI 2.0+), `bd` (beads CLI), `jq` (JSON parsing)

Follow all instructions in AGENTS.md

## Active Technologies
- Bash 4.0+ (POSIX-compatible where possible) + `git` (2.25+), `gh` (GitHub CLI 2.0+), `bd` (beads CLI), `jq` (JSON parsing) (001-git-lifecycle-plugin)
- N/A (relies on git repository and beads `.beads/` directory) (001-git-lifecycle-plugin)

## Recent Changes
- 001-git-lifecycle-plugin: Added Bash 4.0+ (POSIX-compatible where possible) + `git` (2.25+), `gh` (GitHub CLI 2.0+), `bd` (beads CLI), `jq` (JSON parsing)
- 001-git-lifecycle-plugin: Added Bash 4.0+ (POSIX-compatible where possible) + `git` (2.25+), `gh` (GitHub CLI 2.0+), `bd` (beads CLI), `jq` (JSON parsing)
